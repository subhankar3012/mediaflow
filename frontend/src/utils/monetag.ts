/**
 * Monetag Ad Integration
 * Tag is loaded once statically in index.html <head> (zone 285252).
 * triggerMonetagAd is kept as a safe no-op to ensure no duplicate script injections.
 */

export function triggerMonetagAd(): void {
  // Statically loaded in index.html head; no dynamic injection needed.
}

